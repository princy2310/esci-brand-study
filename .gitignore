# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
.next/
out/
.DS_Store

# Local env
.env
.env.local

# Raw row cache and logs from the precompute step. The cache is a fetch
# optimisation, not a source of truth: the committed esci.json is.
scripts/.cache/
scripts/*.log

# Phase 2 scoring checkpoint (partial run; final output is model_eval.json)
dashboard/public/data/*.partial.json
